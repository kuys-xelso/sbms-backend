# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthEntity {
  accessToken: String!
  refreshToken: String!
  user: UserEntity!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DepartmentEntity {
  createdAt: DateTime!
  description: String
  id: String!
  name: String!
  updatedAt: DateTime!
}

type Mutation {
  """Create department (admin only)"""
  createDepartment(description: String, name: String!): DepartmentEntity!

  """Delete user (admin only)"""
  deleteUser(id: String!): Boolean!

  """Get new access token using refresh token"""
  refreshAccessToken(refreshToken: String!): String!

  """Login with email and password"""
  signin(input: SignInInput!): AuthEntity!

  """Register new user account"""
  signup(input: SignUpInput!): AuthEntity!

  """Update current user profile"""
  updateProfile(firstName: String, lastName: String): UserEntity!

  """Update user (admin only)"""
  updateUser(firstName: String, id: String!, lastName: String, role: UserRole): UserEntity!
}

type Query {
  """List departments"""
  departments: [DepartmentEntity!]!
  healthCheck: String!

  """Get current logged-in user"""
  me: UserEntity!

  """Get user by ID"""
  user(id: String!): UserEntity

  """List all users (admin only)"""
  users(skip: Int, take: Int): [UserEntity!]!
}

input SignInInput {
  email: String!
  password: String!
}

input SignUpInput {
  email: String!
  firstName: String!
  lastName: String
  password: String!
}

type UserEntity {
  createdAt: DateTime!
  email: String!
  firstName: String!
  id: String!
  lastName: String
  role: UserRole!
  updatedAt: DateTime!
}

enum UserRole {
  ADMIN
  USER
}