# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthEntity {
  accessToken: String!
  refreshToken: String!
  user: User!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Department {
  createdAt: DateTime!
  description: String
  id: String!
  name: String!
  updatedAt: DateTime!
}

type Mutation {
  """Create department (admin only)"""
  createDepartment(description: String, name: String!): Department!

  """Delete user (admin only)"""
  deleteUser(id: String!): Boolean!

  """Logout current user and invalidate refresh token"""
  logout: Boolean!

  """Rotate tokens using refresh token"""
  refreshAccessToken(refreshToken: String!): AuthEntity!

  """Login with email and password"""
  signin(input: SignInInput!): AuthEntity!

  """Register new user account"""
  signup(input: SignUpInput!): AuthEntity!

  """Update current user profile"""
  updateProfile(firstName: String, lastName: String): User!

  """Update user (admin only)"""
  updateUser(firstName: String, id: String!, lastName: String, role: UserRole): User!
}

type Query {
  """List departments"""
  departments: [Department!]!
  healthCheck: String!

  """Get current logged-in user"""
  me: User!

  """Get user by ID"""
  user(id: String!): User

  """List all users (admin only)"""
  users(skip: Int, take: Int): [User!]!
}

input SignInInput {
  email: String!
  password: String!
}

input SignUpInput {
  email: String!
  firstName: String!
  lastName: String
  password: String!
}

type User {
  createdAt: DateTime!
  email: String!
  firstName: String!
  id: String!
  lastName: String
  role: UserRole!
  updatedAt: DateTime!
}

enum UserRole {
  ADMIN
  USER
}